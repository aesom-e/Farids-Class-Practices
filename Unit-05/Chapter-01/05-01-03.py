def area(x: int, y: int) -> int:
    return x * y

def perimeter(x: int, y: int) -> int:
    return x*2 + y*2

def volume(x: int, y: int, z: int) -> int:
    return x * y * z

def surfaceArea(x: int, y: int, z: int) -> int:
    return x * y * 2 + y * z * 2 + x * z * 2